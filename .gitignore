# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment secrets — never commit real credentials
.env
.env.local
.env.*.local

# Database (SQLite file + Prisma migration engine files)
prisma/dev.db
prisma/dev.db-journal
prisma/*.db
prisma/*.db-journal

# Uploaded media (user content, not source)
public/uploads/*
!public/uploads/.gitkeep

# Logs
npm-debug.log*
*.log

# OS/editor cruft
.DS_Store

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Auto-regenerated by Next.js on every "dev"/"build" run (its content
# toggles depending on which one ran last) — never meaningful to commit
next-env.d.ts
